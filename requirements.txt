# Dependency metadata now lives in pyproject.toml. This shim keeps the
# documented `pip install -r requirements.txt` working — it installs PLASMA
# editable with every plugin extra plus the build/test tooling.
-e .[all,build,test]
