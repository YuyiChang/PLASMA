# Playbook screenshots are binary and regenerated by scripts/capture_screenshots.py —
# keep them out of the packed git history via Git LFS.
docs/assets/screenshots/**.png filter=lfs diff=lfs merge=lfs -text
